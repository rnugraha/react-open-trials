{
  "name": "apollo-server",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "dependencies": {
    "apollo-datasource-rest": "^0.5.0",
    "apollo-server": "^2.6.3",
    "graphql": "^14.3.1"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/*.js"
    ]
  },
  "devDependencies": {
    "jest": "^24.8.0"
  }
}
